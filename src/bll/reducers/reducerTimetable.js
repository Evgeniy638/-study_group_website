const initialState = {

}

const reducerTimetable = (state=initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default reducerTimetable